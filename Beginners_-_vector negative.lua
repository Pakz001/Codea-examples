-- negative vector

-- Use this function to perform your initial setup
function setup()
    print("Hello World!")
    -- create a new vector in a with the negative values from the set vector
    a = -vec2(10,10)
    -- print out the vector values inside the console window.
    print(a)
end

-- This function gets called once every frame
function draw()
    -- This sets a dark background color 
    background(40, 40, 50)

    -- This sets the line thickness
    strokeWidth(5)

    -- Do your drawing here
    
end

